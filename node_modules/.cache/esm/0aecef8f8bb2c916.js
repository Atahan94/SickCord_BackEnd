let createServer,updateUser;_8bb‍.x([["default",()=>_8bb‍.o]]);_8bb‍.w("../../../controller/server",[["createServer",["createServer"],function(v){createServer=v}]]);_8bb‍.w("../../../controller/user",[["updateUser",["updateUser"],function(v){updateUser=v}]]);


const create =  async (req, res) => {
    try {
      const {name} = req.body
      const image = req.file;
      
      const {serverID} = await createServer({name, owner: req.session.user.id, image});
      
      const updatedUser = await updateUser(req.session.user.id, { $push: { servers: serverID } }); //FİX THE UPDATEUSER FUNCTİON
      res.status(200).json({updatedUser});
      return Promise.resolve();
    } catch (error) {
      _8bb‍.g.console.log("error on the request", error)
      res.status(500).json({code: error});
    }
  };

  _8bb‍.d(create);
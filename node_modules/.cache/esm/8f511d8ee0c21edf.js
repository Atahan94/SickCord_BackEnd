let Schema,model,channelSchema;_e0c‍.x([["default",()=>_e0c‍.o]]);_e0c‍.w("mongoose",[["Schema",["Schema"],function(v){Schema=v}],["model",["model"],function(v){model=v}]]);_e0c‍.w("./serverUtilities",[["channelSchema",["channelSchema"],function(v){channelSchema=v}]]);


const serverSchema = new Schema({
    name: {
      type: String,
      required: true,
    },
    owner: {
      type: String,
      required: true,
    },
    createdAt: { type: Date, default: Date.now },
    members: [{
      type: Schema.Types.ObjectId,
      ref: 'Users',
    }],
    channels: [channelSchema],
  });

const Server = model('Servers', serverSchema);

_e0c‍.d(Server);


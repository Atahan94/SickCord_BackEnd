let createClient;_097‍.x([["default",()=>_097‍.o]]);_097‍.w("redis",[["createClient",["createClient"],function(v){createClient=v}]]);

const redisClient = createClient({
    url: 'redis://127.0.0.1:6379' // Redis server URL
    // password: 'yourpassword' // Uncomment and set your password if you configured one
});

// Handle Redis connection errors
redisClient.on('error', (err) => {
    _097‍.g.console.error('Redis error:', err);
});


_097‍.d(redisClient);

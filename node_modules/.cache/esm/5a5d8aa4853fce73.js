let updateServer;_853‍.x([["default",()=>_853‍.o]]);_853‍.w("../../../../controller/server",[["updateServer",["updateServer"],function(v){updateServer=v}]]);

const createGroup =  async (req, res) => {
    try {
      const {id} = req.params
      const {name} = req.body
      
      const groupInfo = await  updateServer({id, contents:{$push: { groups:{ name, channels: [] }}}})
      
      res.status(200).json({groupInfo});
      return Promise.resolve();
    } catch (error) {
      _853‍.g.console.log("error on the request", error)
      res.status(500).json({code: error});
    }
  };

  _853‍.d(createGroup);
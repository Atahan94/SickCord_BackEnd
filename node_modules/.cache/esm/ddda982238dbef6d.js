let updateServer;_38d‍.x([["default",()=>_38d‍.o]]);_38d‍.w("../../../../controller/server",[["updateServer",["updateServer"],function(v){updateServer=v}]]);

const editChannel =  async (req, res) => {
    try {
      const {id, groupID, channelID} = req.params
      const {name, type} = req.body
      
      _38d‍.g.console.log("SERVERID", id, "GROUPID", groupID, "CHANNELID", channelID);
      const contents = {
        $set: {
          "groups.$[group].channels.$[channel].name": name,
          "groups.$[group].channels.$[channel].type": type
        }
      };

      const options = {
        arrayFilters: [
          { "group._id": groupID },  // Targeting the group by its ID
          { "channel._id": channelID }  // Targeting the channel by its ID
        ]
      };

      const groupInfo = await updateServer({
        id,
        contents,
        options
      });
  
      
      res.status(200).json({groupInfo}); 
      return Promise.resolve();
    } catch (error) {
      _38d‍.g.console.log("error on the request", error)
      res.status(500).json({code: error});
    }
  };

  _38d‍.d(editChannel);
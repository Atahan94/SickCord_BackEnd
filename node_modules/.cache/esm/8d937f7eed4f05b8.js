let updateServer;_ed4‍.x([["default",()=>_ed4‍.o]]);_ed4‍.w("../../../../controller/server",[["updateServer",["updateServer"],function(v){updateServer=v}]]);

const deleteChannel =  async (req, res) => {
    try {
      const {id, groupID, channelID} = req.params
      
      
      _ed4‍.g.console.log("SERVERID", id, "GROUPID", groupID, "CHANNELID", channelID);
      const contents = {
        $pull: {
          "groups.$[group].channels": { _id: channelID }
        }
      };

      const options = {
        arrayFilters: [
          { "group._id": groupID },  // Targeting the group by its ID
        ]
      };

      const groupInfo = await updateServer({
        id,
        contents,
        options
      });
  
      
      res.status(200).json({groupInfo}); 
      return Promise.resolve();
    } catch (error) {
      _ed4‍.g.console.log("error on the request", error)
      res.status(500).json({code: error});
    }
  };

  _ed4‍.d(deleteChannel);
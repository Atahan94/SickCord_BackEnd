let createServer,updateUser;_f4a‍.x([["default",()=>_f4a‍.o]]);_f4a‍.w("../../../controller/server",[["createServer",["createServer"],function(v){createServer=v}]]);_f4a‍.w("../../../controller/user",[["updateUser",["updateUser"],function(v){updateUser=v}]]);


const create =  async (req, res) => {
    try {
      const {name} = req.body
     /*  console.log("REQUEST name", req.session) */
      const {serverID} = await createServer({name, owner: req.session.user.id});
      /* console.log("NAME",serverID) */
      
      const updatedUser = await updateUser(req.session.user.id, { $push: { servers: serverID } }); //FİX THE UPDATEUSER FUNCTİON
      res.status(200).json({updatedUser});
      return Promise.resolve();
    } catch (error) {
      _f4a‍.g.console.log("error on the request", error)
      res.status(500).json({code: error});
    }
  };

  _f4a‍.d(create);